#!/usr/bin/env python
# vim:fileencoding=utf-8
from calibre.web.feeds.news import BasicNewsRecipe

class AdvancedUserRecipe1776352268(BasicNewsRecipe):
    title          = 'GNN News'
    language = 'zh' # 语言
    encoding = 'utf-8' # 编码
    utc_offset = 15
    oldest_article = 2
    max_articles_per_feed = 80
    auto_cleanup   = True
    compress_news_images = 2

    feeds          = [
        ('GNN News', 'https://gnn.gamer.com.tw/rss.xml'),
    ]
